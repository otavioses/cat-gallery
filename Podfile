# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'cat-gallery' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for cat-gallery
  pod 'Alamofire', '~> 5.2'
  pod 'RxAlamofire'
  pod 'RxSwift', '~> 5'
  pod 'SwiftyJSON', '~> 4.0'

  target 'cat-galleryTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'cat-galleryUITests' do
    # Pods for testing
  end

end
